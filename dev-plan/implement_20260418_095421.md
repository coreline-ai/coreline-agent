# implement_20260418_095421.md

작성 일시: `2026-04-18 09:54:21 KST`

이 문서는 현재 `coreline-agent` 작업 트리에 누적된 대규모 변경분을 기능군 단위로 정리하고, 리뷰/커밋/머지 가능한 상태로 고정하기 위한 작업 문서다.

## 개발 목적
- 현재 누적된 변경분을 기능군별로 분류하고 리뷰 가능한 단위로 정리한다.
- 새 기능 추가보다 먼저 “무엇이 이미 들어갔는지”를 명확히 만든다.
- 이후 smoke / proxy / planner 후속 작업이 안전하게 이어질 수 있는 기준선을 만든다.

## 개발 범위
- 작업 트리 변경분 inventory 정리
- 기능군별 커밋/리뷰 단위 분리 전략 수립
- 문서/코드/테스트의 연결 관계 정리
- 필요한 최소 검증(typecheck/build/test) 재실행
- 정리 결과를 후속 dev-plan과 연결

## 제외 범위
- 신규 기능 추가
- 대규모 리팩터링
- planner 품질 고도화 자체 구현
- proxy human input mode 구현
- cloud provider 신규 지원 추가

## 참조 문서
- [README](README.md)
- [병렬 개발 마스터플랜](dev-plan/implement_20260414_215534.md)
- [품질 개선 3단계](dev-plan/implement_20260415_205850.md)
- [AgentTool MVP](dev-plan/implement_20260415_205919.md)
- [AgentTool v2](dev-plan/implement_20260416_184609.md)
- [Proxy 운영 polish](dev-plan/implement_20260418_082603.md)
- [AgentTool v3](dev-plan/implement_20260418_082604.md)

## 공통 진행 규칙
- 새 기능을 얹지 말고 이미 구현된 변경분을 정리하는 데 집중한다.
- 기능군 분리는 실제 파일 묶음/테스트 묶음과 맞아야 한다.
- 하나의 정리 단위는 가능한 한 독립적으로 검증 가능해야 한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.

## Phase 상태 요약
- [x] Phase 1 완료
- [x] Phase 2 완료
- [x] Phase 3 완료

## Phase 1. 변경분 inventory 고정
### 목표
- 현재 누적된 변경을 기능군별로 분류하고 정리 범위를 먼저 고정한다.

### 구현 태스크
- [x] 작업 트리 변경 파일을 기능군별로 분류
- [x] proxy / plan / MCP / AgentTool / TUI / session / docs 묶음 정리
- [x] 각 기능군의 핵심 파일과 대표 테스트 파일 매핑
- [x] 기능군 간 공유 파일과 충돌 지점 식별

### 자체 테스트
- [x] 기능군별 대표 파일 목록 문서화
- [x] 각 기능군이 대응하는 테스트 세트와 연결되는지 확인

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 현재 변경분의 구조가 설명 가능한 상태가 된다.
- [x] 다음 Phase 진행 가능

### 2026-04-18 inventory 결과

| 기능군 | 대략 파일 수 | 대표 파일 | 대표 테스트/문서 |
|---|---:|---|---|
| providers/runtime | 15+ | `src/providers/*`, `src/index.ts`, `src/agent/system-prompt.ts` | `tests/phase1-providers.test.ts`, `tests/openai-message-conversion.test.ts` |
| plan mode | 11 | `src/agent/plan-execute/*`, `src/tui/slash-commands.ts`, `src/tui/repl.tsx` | `tests/plan-execute-*.test.ts`, `tests/tui-plan-mode.test.ts` |
| AgentTool / delegation | 8+ | `src/agent/subagent-*`, `src/tools/agent/*` | `tests/agent-tool.test.ts`, `tests/subagent-runtime.test.ts` |
| proxy | 11+ | `src/proxy/*`, `src/proxy-cli.ts` | `tests/proxy-*.test.ts`, `docs/proxy-operations.md` |
| MCP | 8+ | `src/mcp/*`, `src/tools/mcp/*` | `tests/mcp-*.test.ts`, `docs/mcp-ops.md` |
| session / TUI / permissions | 10+ | `src/session/*`, `src/tui/*`, `src/permissions/engine.ts` | `tests/fix-tui-session.test.ts`, `tests/permission-ui.test.ts`, `tests/phase3-permissions.test.ts` |
| smoke / docs / meta | 20+ | `README.md`, `package.json`, `.github/*`, `dev-plan/*` | `tests/provider-smoke.test.ts`, `docs/provider-smoke-*.md` |

### 공유 파일 / 충돌 지점
- `src/index.ts`
- `src/agent/context.ts`
- `src/agent/loop.ts`
- `src/permissions/engine.ts`
- `src/tui/repl.tsx`
- `src/providers/types.ts`

위 파일들은 여러 기능군이 동시에 걸쳐 있어서, 기능군별 commit으로 완전히 분리하기보다 **통합 runtime commit** 또는 **마지막 integration commit**에서 묶는 편이 안전하다.

## Phase 2. 커밋 / 리뷰 단위 설계
### 목표
- 기능군별로 어떤 순서로 커밋/리뷰/머지할지 실행 가능한 순서를 고정한다.

### 구현 태스크
- [x] 최소 3~6개 커밋 단위 제안
- [x] 공유 파일이 많은 묶음은 통합 커밋 기준 정리
- [x] 문서-only / infra-only / runtime-only 경계 정의
- [x] 각 단위별 필수 검증 명령 고정

### 자체 테스트
- [x] 각 단위가 독립적으로 설명 가능한지 검토
- [x] 커밋 단위 순서가 실제 의존성과 맞는지 확인

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 리뷰 가능한 커밋 단위와 순서가 정리된다.
- [x] 다음 Phase 진행 가능

### 제안 커밋 / 리뷰 단위
1. **providers/runtime plumbing**
   - 범위: provider registry, CLI/OAuth providers, OpenAI message conversion, config schema
   - 검증: `bun test tests/phase1-providers.test.ts tests/openai-message-conversion.test.ts tests/cloud-oauth-providers.test.ts`
2. **plan mode + session**
   - 범위: `src/agent/plan-execute/*`, `src/session/*`, TUI `/plan`
   - 검증: `bun test tests/plan-execute-*.test.ts tests/tui-plan-mode.test.ts`
3. **AgentTool + permissions + TUI**
   - 범위: subagent runtime, agent tool, permission UI, session debug
   - 검증: `bun test tests/agent-tool.test.ts tests/subagent-runtime.test.ts tests/permission-ui.test.ts tests/phase3-permissions.test.ts`
4. **proxy**
   - 범위: `src/proxy/*`, `src/proxy-cli.ts`, proxy docs/tests
   - 검증: `bun test tests/proxy-*.test.ts`
5. **MCP**
   - 범위: `src/mcp/*`, `src/tools/mcp/*`, MCP docs/tests
   - 검증: `bun test tests/mcp-*.test.ts`
6. **docs / smoke / final integration**
   - 범위: README, provider smoke docs, `.github`, dev-plan updates, shared runtime glue
   - 검증: `bun run typecheck && bun run build && bun test`

## Phase 3. 기준선 검증 및 후속 연결
### 목표
- 현재 정리안을 실제 검증 기준과 연결하고 다음 후속 문서로 넘길 수 있게 만든다.

### 구현 태스크
- [x] `bun run typecheck` / `bun run build` / `bun test` 기준선 재확인
- [x] 후속 smoke / proxy / planner dev-plan과 연결 지점 정리
- [x] 정리 결과를 문서에 반영

### 자체 테스트
- [x] 전체 검증 통과 여부 재기록
- [x] 후속 2개 이상 dev-plan과 연결 관계 확인

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 현재 워킹트리 정리 전략이 확정된다.
- [x] 이후 후속 작업이 기능 추가보다 정리 기준을 공유할 수 있다.

### 기준선 검증
- `bun run typecheck` ✅
- `bun run build` ✅
- `bun test` ✅ (`368 pass / 0 fail`)

### 후속 연결
- 실환경 smoke 마감: `dev-plan/implement_20260418_095422.md`
- proxy 제약 계약 정리: `dev-plan/implement_20260418_095423.md`
- planner 품질 고도화는 별도 후속 범위로 유지

### 사후 메모 (2026-04-18)
- 위 6개 커밋 단위 제안은 실제로 **기능군별 그룹 커밋 6개**로 정리 후 push 완료.
- 공유 파일(`index.ts`, `loop.ts`, `repl.tsx`, `types.ts` 등)이 많아도, 부분 staging + stash 복원 전략으로 분리 가능함을 확인.
- 향후 유사 규모 변경 시에도 공유 파일이 3곳 이상이면 **우선 통합 위험을 평가하고**, 가능하면 그룹 커밋으로 정리하는 방식을 우선 검토.
