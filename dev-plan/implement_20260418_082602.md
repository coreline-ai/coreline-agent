# implement_20260418_082602.md

작성 일시: `2026-04-18 08:26:02 KST`

이 문서는 provider smoke 운영/CI 자동화 작업의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적
provider smoke의 실행 진입점을 정리하고, CI-safe / manual / nightly-safe를 분리해 운영 가능하게 만든다.

## 개발 범위
- provider smoke 실행 스크립트 정리
- env-gated 규칙 명시
- CI-safe / manual / nightly-safe 분리
- provider smoke checklist 문서 업데이트
- GitHub Actions workflow 초안 추가

## 제외 범위
- 신규 provider 기능 추가
- proxy / MCP / plan mode / TUI 구현 변경
- provider 자체 모델 로직 변경
- 장기 대화용 smoke 확장

## 참조 문서
- [Provider Smoke Checklist](docs/provider-smoke-checklist.md)
- [Provider Smoke CI / Automation](docs/provider-smoke-ci.md)
- [이전 smoke 안정화 계획](dev-plan/implement_20260417_234802.md)

## 공통 진행 규칙
- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- smoke는 실제 호출 여부와 skip/blocked 사유를 분명하게 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.

## Phase 상태 요약
- [x] Phase 1 완료
- [x] Phase 2 완료
- [x] Phase 3 완료

## Phase 1. smoke entry point 정리
### 목표
- package scripts와 smoke target 규칙을 정리해 실행 경로를 단순화한다.

### 구현 태스크
- [x] `package.json`에 ci-safe/manual/nightly-safe smoke scripts 추가
- [x] target별 실행 예시를 checklist에 반영
- [x] `tests/provider-smoke.test.ts`의 target gate가 entry point와 일치하는지 확인

### 자체 테스트
- [x] `bun test tests/provider-smoke.test.ts`
- [x] `bun run smoke:provider:ci-safe`
- [x] `bun run smoke:provider:compatible`

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] smoke 실행 경로가 script 수준에서 정리된다.
- [x] target gate 규칙이 문서와 테스트에서 일치한다.

## Phase 2. CI-safe / manual / nightly-safe 분리
### 목표
- 외부 키가 없어도 안전한 CI 경로와, 수동/야간 경로를 분리한다.

### 구현 태스크
- [x] CI-safe workflow job 추가
- [x] manual smoke workflow_dispatch job 추가
- [x] nightly-safe schedule job 추가
- [x] skip / blocked / out-of-scope 출력 기준 정리

### 자체 테스트
- [x] workflow YAML 문법과 job 분기 확인
- [x] `bun run smoke:provider:cloud`
- [x] `bun run smoke:provider:nightly-safe`

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] CI-safe / manual / nightly-safe가 서로 분리된다.
- [x] 외부 키가 없을 때도 CI-safe 경로가 안전하다.

## Phase 3. 문서/회귀 고정
### 목표
- smoke 운영 기준과 실행 예시를 문서화해 다음 개발의 기준점을 만든다.

### 구현 태스크
- [x] `docs/provider-smoke-checklist.md` 갱신
- [x] `docs/provider-smoke-ci.md` 정리
- [x] workflow에서 사용하는 env/secret/vars 이름을 문서화

### 자체 테스트
- [x] `bun run typecheck`
- [x] `bun run build`
- [x] `bun test tests/provider-smoke.test.ts`

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] smoke 운영 문서가 실행 가능한 상태가 된다.
- [x] 다음 기능 개발 전에 다시 사용할 수 있는 자동화 기준선이 생긴다.

## 2026-04-18 실행 로그

### 구현/문서 변경
- `package.json`에 smoke scripts 추가 ✅
- `.github/workflows/provider-smoke.yml` 추가 ✅
- `docs/provider-smoke-ci.md` 추가 ✅
- `docs/provider-smoke-checklist.md` 갱신 ✅
- `tests/provider-smoke.test.ts` target gate 정리 ✅

### 검증
- `bun test tests/provider-smoke.test.ts` ✅
- `bun run smoke:provider:ci-safe` ✅
- `bun run smoke:provider:manual` ✅
- `bun run smoke:provider:compatible` ✅
- `bun run smoke:provider:cloud` ✅
- `bun run smoke:provider:nightly-safe` ✅
- `bun run build` ✅
- `bun run typecheck` ✅

### blocked / out-of-scope
- Anthropic/OpenAI/Gemini cloud smoke: **BLOCKED**
  - 이유: 2026-04-17 기준 이 환경에 `ANTHROPIC_API_KEY`, `OPENAI_API_KEY`, `GOOGLE_API_KEY`가 없음
- `chatgpt` / `claude` provider smoke: **OUT-OF-SCOPE**
  - 이유: 현재 `tests/provider-smoke.test.ts`는 Anthropic/OpenAI/Gemini/OpenAI-compatible 하니스만 다룸
